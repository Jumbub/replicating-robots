require("lib")
require("common")

local main = function()
	-- c.state.reset()

	-- c.tree.chop({ first = true })
	-- c.tree.harvestTil(function()
	-- 	return c.fuel.available() > 100
	-- end)
	-- c.scan.ground(4)
	-- c.mine.vertical(6)
end

-- main()
