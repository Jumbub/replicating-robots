{
  "license": "MIT",
  "scripts": {
    "build": "tstl",
    "sync": "ls build.lua | entr sh -c \"cp build.lua `head -n 1 .computerDirectory`out.lua && date +%s\" || echo \"No output to sync\"",
    "dev": "tstl --watch"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "concurrently": "^5.3.0",
    "jest": "^26.6.1",
    "lua-types": "^2.8.0",
    "ts-jest": "^26.4.2",
    "typescript": "^4.0.3",
    "typescript-to-lua": "^0.36.0"
  }
}
